spring:
  datasource: 
    url: jdbc:mysql://localhost/db_disney?serverTimezone=Europe/Madrid
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create-drop
  mvc:
    contentnegotiation:
      favor-parameter: true
logging:
  level:
    sql: debug